resize()[

	capacity = capacity + 5;
	employee* new employee = new employee[capcity]
		for (inti = 0, i < currentlyenrolled;i++)
			newemployees[i].setemployid(employ[i].getid)
			newemployess[i].setemployname(employ[i].getid)

			delete[]employees;
	employees = new employees;

	insert
		if (capacity == size)
		{
			resize
				employees[currentlyemployes].setempid(empid);
			emploees[currentlyenrolles].setempname(emplname)
				currentlyemployed++
		}